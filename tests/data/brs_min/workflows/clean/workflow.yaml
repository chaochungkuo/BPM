id: clean
description: Example workflow
params:
  name: {type: str, required: true}
render:
  into: "${ctx.project.name}/${ctx.template.id}/"
  files:
    - out.txt.j2 -> out.txt
    - run.sh.j2 -> run.sh
run:
  entry: "run.sh"

